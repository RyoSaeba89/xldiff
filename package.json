{
  "name": "xldiff",
  "version": "2.0.0",
  "private": true,
  "description": "XLDiff — comparateur de fichiers Excel (build de l'exe de bureau Tauri)",
  "scripts": {
    "dist": "node scripts/make-dist.js",
    "exe": "npm run dist && tauri build --no-bundle",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
