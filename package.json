{
  "name": "xldiff",
  "version": "3.3.0",
  "private": true,
  "description": "XLDiff — comparateur de fichiers Excel (build de l'exe de bureau Tauri)",
  "scripts": {
    "dist": "node scripts/make-dist.js",
    "exe": "npm run dist && cd src-tauri && cargo build --release",
    "sign": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/sign-exe.ps1",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
